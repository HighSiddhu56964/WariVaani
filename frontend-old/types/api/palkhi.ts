export interface PalkhiResponse {
  id: number;
  name: string;
  saint: string;
  current_checkpoint: string;
  next_checkpoint: string;
  speed: string;
  lat: number;
  lng: number;
  last_updated: string;
}
