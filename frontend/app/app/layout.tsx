"use client";

import WarkariLayout from "../(warkari)/layout";

export default function AppLayout({ children }: { children: React.ReactNode }) {
  return <WarkariLayout>{children}</WarkariLayout>;
}
